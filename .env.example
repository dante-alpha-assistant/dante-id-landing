# ============================================
# DANTE ID - Environment Variables
# ============================================
# Copy to .env (server/) and set values

# --- Supabase (required) ---
SUPABASE_URL=https://your-project.supabase.co        # Supabase project URL
SUPABASE_SERVICE_KEY=eyJ...                           # Supabase service_role key (server-side only, NOT anon)

# --- Frontend (set in root .env or Vercel env) ---
VITE_SUPABASE_URL=https://your-project.supabase.co   # Same as SUPABASE_URL
VITE_SUPABASE_ANON_KEY=eyJ...                         # Supabase anon/public key

# --- AI Generation (required) ---
OPENROUTER_API_KEY=sk-or-v1-...                       # OpenRouter API key for AI deliverable generation

# --- Stripe Billing (required for paid plans) ---
STRIPE_SECRET_KEY=sk_test_...                         # Stripe secret key
STRIPE_WEBHOOK_SECRET=whsec_...                       # Stripe webhook signing secret
STRIPE_PRO_PRICE_ID=price_...                         # Stripe Price ID for Pro plan
STRIPE_TEAM_PRICE_ID=price_...                        # Stripe Price ID for Team plan

# --- Landing Page Deployment (optional) ---
GH_TOKEN=ghp_...                                      # GitHub personal access token (repo scope) for pushing generated landing pages
VERCEL_TOKEN=...                                      # Vercel token for deploying generated landing pages

# --- Email Notifications (optional) ---
RESEND_API_KEY=re_...                                 # Resend API key for transactional emails
FROM_EMAIL="DANTE ID <notifications@dante.id>"        # From address for emails

# --- App Config ---
APP_URL=https://dante.id                              # Public app URL (used in emails, Stripe redirects)

# --- Fleet / Discord (optional) ---
DISCORD_BOT_TOKEN=...                                 # Discord bot token for agent activity feeds
